<%= form_for(@warning_event) do |f| %>
  <% if @warning_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@warning_event.errors.count, "error") %> prohibited this warning_event from being saved:</h2>

      <ul>
      <% @warning_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
