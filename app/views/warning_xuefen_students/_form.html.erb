<%= form_for(@warning_xuefen_student) do |f| %>
  <% if @warning_xuefen_student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@warning_xuefen_student.errors.count, "error") %> prohibited this warning_xuefen_student from being saved:</h2>

      <ul>
      <% @warning_xuefen_student.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
